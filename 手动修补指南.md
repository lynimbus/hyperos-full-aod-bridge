# 手动修补指南（纯手工流程）

> 本文件为 `build.sh` 与 `patch_smali.py` 自动化流程的手工等价方案：在不依赖本仓库脚本
> 的前提下，通过命令与文本编辑器完成同样的两处 smali 注入。适用于需要理解每一处改动的
> 含义，或在没有 Python 环境的机器上操作的情形。
>
> **版本绑定声明**：以下所有方法签名、字段名、`.registers` 数值均来自已验证机型
> Xiaomi 14（`houji`，Android 16，`OS3.0.303.0.WNCCNXM`）的 `services.jar`。
> 更换任何固件前必须先进行反汇编核对，若存在不一致应停止操作，不得强行修改。

## 目录

1. [原理速览](#1-原理速览)
2. [准备工作](#2-准备工作)
3. [流程总览](#3-流程总览)
4. [第一步：拆包提取 classes2.dex](#4-第一步拆包提取-classes2dex)
5. [第二步：反汇编](#5-第二步反汇编)
6. [第三步：编译 AodDozeBridge 类](#6-第三步编译-aoddozebridge-类)
7. [第四步：手工注入（核心）](#7-第四步手工注入核心)
8. [第五步：回编译](#8-第五步回编译)
9. [第六步：重打包 services.jar](#9-第六步重打包-servicesjar)
10. [刷入与安装](#10-刷入与安装)
11. [验证与调参](#11-验证与调参)
12. [禁忌清单](#12-禁忌清单)

---

## 1. 原理速览

全屏 AOD 下智能显示熄灭面板后，恢复时面板仍停留在 LP1（doze）状态，此时内核拒绝普通
背光写入（dmesg 中表现为 `skip set backlight ... due to LP1 on`），屏幕无法重新点亮。

修复思路：新增 `AodDozeBridge` 类，在显示设备回调 `LocalDisplayAdapter$LocalDisplayDevice$1`
中注入两处：

| 注入点 | 作用 |
| --- | --- |
| `setDisplayState(I)V` 开头 | 仅记录 OFF→DOZE 恢复边沿，不操作面板 |
| `setDisplayBrightness(FF)V` | 原方法改名，新增同名包装：写入亮度前先将面板切回 `NORMAL`，写入后切回 `DOZE` |

时序：记录边沿 → 写入亮度时切换为 `NORMAL`，等待 16 ms → 写入亮度 → 等待 64 ms → 切回 `DOZE`。

**为何必须跨两个事件记录状态**：`DisplayPowerController` 将一次恢复拆分为两个请求，
状态请求携带的亮度为 `-1.0`（实机 9/10 次如此），真实息屏亮度约 70 ms 后作为独立
请求送达。因此状态点无法取得亮度值，必须将边沿存入桥接类的 `sPendingEdge` 静态字段，
待下一次亮度请求到来时消费。

**为何需要两个延迟**：电源模式与亮度均为异步 SurfaceControl 事务。实测 NORMAL 约 27 ms
生效、亮度写入约 50 ms 生效，因此写入亮度前需等待（`PANEL_SETTLE_MS = 16`），写入后需
等待亮度实际到达内核再切回 DOZE（`COMMIT_DELAY_MS = 64`）。二者为可调参数，详见
[第 11 节](#11-验证与调参)。

---

## 2. 准备工作

### 2.1 工具

| 工具 | 版本 | 用途 |
| --- | --- | --- |
| JDK 17 | 17.x | 编译桥接类、运行 smali 工具链 |
| smali / baksmali 工具链 | 3.0.9 | 反汇编 / 回编译 dex |
| r8 | 9.1.31 | 提供 d8，把 class 打成 dex |
| Python 3 | 任意 | 拆包 / 重打包辅助（可换成 `unzip` / `zip`） |
| 文本编辑器 | 任意 | 手工改 smali |

smali 工具链依赖若干 jar（guava、jcommander、jsr305、antlr-runtime、antlr、
stringtemplate），请将以下文件全部放入同一目录（下文记为 `$TOOLS`）：

```text
https://maven.google.com/com/android/tools/smali/smali-baksmali/3.0.9/smali-baksmali-3.0.9.jar
https://maven.google.com/com/android/tools/smali/smali/3.0.9/smali-3.0.9.jar
https://maven.google.com/com/android/tools/smali/smali-dexlib2/3.0.9/smali-dexlib2-3.0.9.jar
https://maven.google.com/com/android/tools/smali/smali-util/3.0.9/smali-util-3.0.9.jar
https://maven.google.com/com/android/tools/r8/9.1.31/r8-9.1.31.jar
https://repo1.maven.org/maven2/com/google/guava/guava/31.1-android/guava-31.1-android.jar
https://repo1.maven.org/maven2/com/beust/jcommander/1.64/jcommander-1.64.jar
https://repo1.maven.org/maven2/com/google/code/findbugs/jsr305/3.0.2/jsr305-3.0.2.jar
https://repo1.maven.org/maven2/org/antlr/antlr-runtime/3.5.2/antlr-runtime-3.5.2.jar
https://repo1.maven.org/maven2/org/antlr/antlr/3.5.2/antlr-3.5.2.jar
https://repo1.maven.org/maven2/org/antlr/stringtemplate/3.2.1/stringtemplate-3.2.1.jar
```

为便于后续调用，定义两个 shell 函数（本仓库 `build.sh` 采用同样做法）：

```bash
CP="$(ls "$TOOLS"/*.jar | tr '\n' ':')"
baksmali() { java -cp "$CP" com.android.tools.smali.baksmali.Main "$@"; }
smali()    { java -cp "$CP" com.android.tools.smali.smali.Main "$@"; }
d8()       { java -cp "$TOOLS/r8-9.1.31.jar" com.android.tools.r8.D8 "$@"; }
```

### 2.2 取原厂 services.jar

必须使用**目标机器自身的** `services.jar`，不可使用其他机型的版本：

```bash
adb pull /system/framework/services.jar
```

```bash
# 核对构建版本，保证与后文的 smali 基线一致
adb shell getprop ro.build.version.incremental   # 期望 OS3.0.303.0.WNCCNXM
adb shell getprop ro.product.device              # 期望 houji
```

### 2.3 目录规划

```text
work/
├── classes/       # javac 输出
├── dex/           # d8 输出
├── bridge/        # AodDozeBridge 反汇编输出
├── smali2/        # classes2.dex 反汇编输出（在这里改）
├── classes2.dex   # 原厂 dex
├── classes2-patched.dex
└── services-patched.jar
```

---

## 3. 流程总览

```text
services.jar ──拆包──▶ classes2.dex ──baksmali──▶ smali2/（目标类在这里）
                                                      │
AodDozeBridge.java ──javac──▶ class ──d8──▶ dex ──baksmali──▶ AodDozeBridge.smali
                                                      │
                              ◀── 手工注入（第四步）──┤
                                                      ▼
smali2/ ──smali 回编译──▶ classes2-patched.dex ──重打包──▶ services-patched.jar
```

---

## 4. 第一步：拆包提取 classes2.dex

在 `services.jar` 中仅需处理 `classes2.dex`（目标类
`LocalDisplayAdapter$LocalDisplayDevice$1` 位于其中；亦可先用 `unzip -l` 确认其所在
dex）。存储模式与对齐问题在重打包时处理，此处直接原样取出：

```bash
python3 - <<'PY'
import zipfile
zipfile.ZipFile('services.jar').read('classes2.dex')
PY
```

或者使用 `unzip`：

```bash
unzip -p services.jar classes2.dex > work/classes2.dex
```

同时确认目标类确实存在于其中：

```bash
unzip -l services.jar | grep classes2.dex
```

> 若目标类不在 classes2.dex 中（固件调整了 dex 布局），请将上述所有 `classes2` 替换为
> 实际所在的 dex 名称。本仓库仅针对 houji 的版本验证过，其他布局需自行排查。

---

## 5. 第二步：反汇编

dex 反汇编的 `-a` 参数按目标系统 API 给定（Android 16 即 API 36，`build.sh` 中的
`SRC_API`）：

```bash
baksmali disassemble -a 36 -o work/smali2 work/classes2.dex
```

完成后应存在：

```bash
ls work/smali2/com/android/server/display/LocalDisplayAdapter\$LocalDisplayDevice\$1.smali
```

---

## 6. 第三步：编译 AodDozeBridge 类

桥接类源码位于仓库 `src/com/android/server/display/AodDozeBridge.java`，另依赖两个
编译期垫片（`stubs/` 中为隐藏 API 提供的实现，仅参与编译，不进入产物）。

```bash
javac -nowarn -XDstringConcat=inline -d work/classes \
  stubs/com/android/server/display/*.java src/com/android/server/display/*.java
d8 --release --min-api 36 --no-desugaring \
  --output work/dex work/classes/com/android/server/display/AodDozeBridge.class
baksmali disassemble -a 36 -o work/bridge work/dex/classes.dex
```

产物 `work/bridge/com/android/server/display/AodDozeBridge.smali` 即后续需放入反汇编树
的文件。**注意必须使用 `-XDstringConcat=inline`**，原因见[第 12 节](#12-禁忌清单)。

> 若不想安装 JDK，也可直接从已构建环境的 `.work/bridge/` 中取用这份 smali，它不依赖
> 工具链；但自行编译更易保证与源码一致。

---

## 7. 第四步：手工注入（核心）

打开 `work/smali2/com/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1.smali`。

### 7.0 核对前置条件

文件中必须且只能各出现一次以下字段（`patch_smali.py` 的检查项，手工操作时同样须满足）：

```text
.field final synthetic val$oldState:I
.field final synthetic val$state:I
.field final synthetic val$token:Landroid/os/IBinder;
.field final synthetic this$1:Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;
```

另需确认：

- `LocalDisplayAdapter$LocalDisplayDevice.smali` 中存在
  `-$$Nest$fgetmPhysicalDisplayId` 和 `getDisplayId(J)I`；
- 整个文件中**没有** `AodDozeBridge` 字样（防止重复打补丁）。

任何一项不一致即应停止，更换正确版本的 jar。

### 7.1 重命名原 setDisplayBrightness

找到方法定义（stock 中的形式如下）：

```smali
.method private setDisplayBrightness(FF)V
    .registers 9
    .param p1, "brightnessState"    # F
    .param p2, "sdrBrightnessState"    # F
```

将第一行改为：

```smali
.method private aodBridgeSetDisplayBrightness(FF)V
    .registers 9
    .param p1, "brightnessState"    # F
    .param p2, "sdrBrightnessState"    # F
```

**方法体不得作任何改动。** 类内原有调用点引用的均为 `setDisplayBrightness(FF)V`
（stock 中为 5 处 `invoke-direct`），重命名后这些调用将自动指向后文新增的同名包装
方法——此即"包装"技巧，无需修改任何调用点。

### 7.2 setDisplayState 开头注入边沿记录

找到：

```smali
.method private setDisplayState(I)V
    .registers 13
    .param p1, "state"    # I
```

在 `.param` 行之后、原方法第一条指令之前插入：

```smali
    invoke-direct {p0}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->aodBridgeDisplayId()I

    move-result v0

    iget v1, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->val$oldState:I

    move v2, p1

    invoke-static {v0, v1, v2}, Lcom/android/server/display/AodDozeBridge;->onDisplayState(III)V
```

寄存器账本：仅使用 v0–v2 三个局部寄存器。`setDisplayState` 的 `.registers 13` 包含参数
（p0、p1 各占一个），剩余 11 个局部寄存器远大于 3，因此**无需修改 `.registers` 行**。
（一般要求 `registers - 2 >= 3`，`build.sh` 的自动注入也以此为下限。）

### 7.3 在文件末尾追加包装方法与辅助方法

定位到文件的最后一个 `.end method`，在其后追加以下整段内容：

```smali
.method private aodBridgeDisplayId()I
    .registers 4

    iget-object v0, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->this$1:Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;

    invoke-static {v0}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;->-$$Nest$fgetmPhysicalDisplayId(Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;)J

    move-result-wide v1

    invoke-virtual {v0, v1, v2}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;->getDisplayId(J)I

    move-result v0

    return v0
.end method

.method private setDisplayBrightness(FF)V
    .registers 9
    .param p1, "brightnessState"    # F
    .param p2, "sdrBrightnessState"    # F

    invoke-direct {p0}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->aodBridgeDisplayId()I

    move-result v0

    iget v1, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->val$state:I

    iget-object v2, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->val$token:Landroid/os/IBinder;

    invoke-static {v0, v1, v2, p1}, Lcom/android/server/display/AodDozeBridge;->beginBrightness(IILandroid/os/IBinder;F)F

    move-result v3

    const/4 v4, 0x0

    cmpl-float v4, v3, v4

    if-lez v4, :aod_bridge_original

    move p1, v3

    move p2, v3

    :aod_bridge_original
    :try_start_aod_bridge
    invoke-direct {p0, p1, p2}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->aodBridgeSetDisplayBrightness(FF)V
    :try_end_aod_bridge
    .catchall {:try_start_aod_bridge .. :try_end_aod_bridge} :catchall_aod_bridge

    invoke-static {}, Lcom/android/server/display/AodDozeBridge;->endBrightness()V

    return-void

    :catchall_aod_bridge
    move-exception v5

    invoke-static {}, Lcom/android/server/display/AodDozeBridge;->endBrightness()V

    throw v5
.end method
```

逐段说明：

- **`aodBridgeDisplayId()I`**：获取逻辑 displayId。`LocalDisplayDevice` 的
  `mPhysicalDisplayId` 为私有字段，只能通过嵌套类合成的 `-$$Nest$fgetmPhysicalDisplayId`
  访问；`getDisplayId(J)` 将其转换为逻辑 id（AodDozeBridge 的 `isFullAodState` 需要
  该值）。寄存器：p0 + v0–v2（v1、v2 为 wide 的 long）。
- **包装方法头部**：`beginBrightness(displayId, val$state, val$token, p1)` 返回一个
  float。返回值 > 0 表示"桥接接管本次亮度"，此时将两个亮度参数均替换为桥接给出的
  亮度；返回 0 表示不接管，按原样调用原方法。
- **`.catchall`**：原方法体抛出异常时也必须执行 `endBrightness()`（将面板送回 DOZE），
  否则面板将停留在 NORMAL 状态。异常路径先将异常记录到 v5，再执行 `throw v5`。
- 寄存器账本（.registers 9）：p0–p2 三个参数 + v0–v5 六个局部。v0 显示 id、v1 状态、
  v2 token、v3 桥接返回值、v4 比较结果、v5 异常。

> **标签名**：`aod_bridge_original`、`try_start_aod_bridge` 等为本类内的标签，
> 只要与文件中已有标签不重名即可（smali 标签属于方法局部作用域，理论上方法内不冲突
> 即可，但保守起见使用该唯一前缀）。

### 7.4 放置桥接类

将第六步生成的 smali 复制到反汇编树中：

```bash
mkdir -p work/smali2/com/android/server/display
cp work/bridge/com/android/server/display/AodDozeBridge.smali \
   work/smali2/com/android/server/display/AodDozeBridge.smali
```

### 7.5 核对改动

```bash
grep -c "AodDozeBridge" \
  work/smali2/com/android/server/display/LocalDisplayAdapter\$LocalDisplayDevice\$1.smali
```

本机基线（houji）的补丁结果为 4 处：`setDisplayState` 注入点的 `onDisplayState`、
包装方法中的 `beginBrightness`，以及两条路径上的 `endBrightness`。另需确认不存在
`invoke-custom` / `invoke-polymorphic`（见[第 12 节](#12-禁忌清单)）：

```bash
grep -rn "invoke-custom\|invoke-polymorphic" work/smali2/com/android/server/display/AodDozeBridge.smali
```

---

## 8. 第五步：回编译

`services.jar` 的 dex 版本为 039，对应 `-a 29`（`build.sh` 中的 `DEX_API`）——**注意
回编译与反汇编的 `-a` 参数不同**：

```bash
smali assemble -a 29 -o work/classes2-patched.dex work/smali2
```

回编译失败即说明上述某处存在错误（寄存器、标签或引用），不存在部分成功的情况。

---

## 9. 第六步：重打包 services.jar

### 9.1 为何不能直接使用 zip

Android 对 boot classpath 上的 jar 有强制要求：

- dex 条目必须是 **stored（不压缩）**；
- 每个 dex 的数据段必须 **4 字节对齐**（未对齐的 jar 在新版 Android 上会被拒绝加载）。

`zip` 默认启用压缩且对齐方式不可控，因此应使用本仓库的 `repack_jar.py`，或手动控制
对齐。`repack_jar.py` 的功能为：保持原 jar 的条目顺序、压缩方式与权限位不变，仅替换
`classes2.dex` 的内容，并为 stored 条目在文件头添加与 zipalign 相同的 extra field
以补齐对齐：

```bash
python3 repack_jar.py services.jar services-patched.jar \
  "classes2.dex=work/classes2-patched.dex"
```

没有 Python 时的等效手工方法如下，先：

```bash
zip -0 -j work/patch.zip work/classes2-patched.dex
```

得到 stored 的 dex（但未 4 字节对齐），再对产物 jar 执行 `zipalign -p 4`。
更简便的做法是直接使用 `build.sh` 中的 `repack_jar.py`，无需手工编写脚本。

### 9.2 校验产物

```bash
unzip -l services-patched.jar | grep dex        # 确认条目都在
zipinfo services-patched.jar | grep classes2    # 确认是 stored
python3 - <<'PY'
import zipfile
z = zipfile.ZipFile('services-patched.jar')
for i in z.infolist():
    if i.filename.endswith('.dex'):
        off = i.header_offset + 30 + len(i.filename) + len(i.extra)
        print(i.filename, i.compress_type == zipfile.ZIP_STORED, off % 4 == 0)
PY
```

三个 dex 均应为 `True True`。原厂的 `classes2.dex` 必须被替换（文件大小会发生变化）；
经 `baksmali disassemble` → `smali assemble` 得到的产物与原厂字节不一致属正常现象——
smali 工具的标签/顺序重排不改变语义（见第 12 节"不可复现"一条）。

---

## 10. 刷入与安装

### 10.1 Magisk 模块方式（推荐，可回滚）

制作 zip 包：将补丁后的 jar 放入 `system/framework/services.jar`，根目录放置
`module.prop` 与 `customize.sh`。`customize.sh` 采用本仓库 `magisk/customize.sh` 的
模板，将 `@STOCK_SHA256@` 替换为原厂 jar 的 sha256：

```bash
sha256sum services.jar
```

`customize.sh` 会在安装时比对设备上当前 jar 的 sha256，不一致则直接 `abort`——此举
用于防止将其他机型的包刷入设备。若此前安装过旧模块，须先卸载旧模块并重启，否则安装
校验无法通过。

### 10.2 直接替换入 ROM

将 `services-patched.jar` 覆盖至 `/system/framework/services.jar`，删除
`/system/framework/oat/arm64/services.{odex,vdex,art}` 以及 `.fsv_meta`，按官改包流程
处理 AVB / dm-verity 签名。恢复时换回原厂 jar 即可，无需重刷分区。

---

## 11. 验证与参数调整

### 11.1 查看日志

```bash
adb shell su -c 'cat /data/system/aod_bridge.log'
```

（`su` 在已验证机型上位于 `/system/bin/su`，为 KernelSU；日志文件权限为
`0600 system:system`，需 root 权限读取。）

正常的恢复过程应产生两行日志：

```text
armed OFF->DOZE edge, display=0 state=3
relighting display 0 state=3 with brightness 0.0304
```

若仅出现第一行，说明边沿已记录但后续亮度请求未匹配（或亮度不可用）。

### 11.2 确认写入已到达面板

```bash
adb shell su -c 'dmesg | grep -iE "backlight|LP1"'
```

`relighting` 之后出现 `set 51 backlight N` 即为成功；出现
`skip set backlight ... due to LP1 on` 则说明写入发生在 LP1 之后，延迟不足。
**注意**：framework 自身的亮度写入在每次恢复时都会被内核以 LP1 为由拒绝，此为原厂
行为，并非本补丁引入的回归。

### 11.3 调整延迟参数

若日志显示 relight 已完成但屏幕仍为黑屏，应调大 `AodDozeBridge.java` 中的两个常量：

```java
private static final long PANEL_SETTLE_MS = 16L;   // 等待 NORMAL 生效
private static final long COMMIT_DELAY_MS = 64L;   // 等待亮度生效后再切回 DOZE
```

修改后需从第 6、8、9 节重新执行编译流程。代价仅为恢复边沿增加几十毫秒延迟。

---

## 12. 禁忌清单

1. **严禁让 `invoke-custom` / `invoke-polymorphic` 进入 dex。** `services.jar` 位于 boot
   classpath 上，ART 解析 `StringConcatFactory` 的 bootstrap method 时会触发
   `Runtime::Abort`——此为 native abort，Java 的 `catch` 与 smali 的 `.catchall` 均
   无法拦截，system_server 将直接崩溃。javac 17 默认将 `"a" + x` 编译为
   `invoke-dynamic`，v1 与 v2.0 即因此崩溃。因此编译桥接类必须携带
   `-XDstringConcat=inline`（回退为 StringBuilder），lambda / 方法引用同理。原厂
   services.jar 中 `StringConcatFactory` 出现 0 次，不得破例。
2. **不得调小 `setDisplayState` 的 `.registers`**。注入仅使用 v0–v2，且方法开头没有
   存活局部变量，因此无需任何修改；若固件版本的方法头发生变化，`registers - 2`
   仍必须 ≥ 3。
3. **`.catchall` 不可或缺**。原方法体抛出异常时若不将面板送回 DOZE，面板将停留在
   NORMAL，屏幕常亮不熄。
4. **注入点不可随意选择**。在 `setDisplayState` 中写入亮度没有意义——该请求的亮度为
   `-1.0`。必须记录边沿并等待亮度请求，这正是 `sPendingEdge` 存在的唯一理由。
   不得"简化"回单点注入。
5. **桥接类每个入口都应吞掉 `Throwable` 并降级为 no-op**——框架不匹配时绝不允许
   引发 bootloop。此为设计约定，不得让这些入口抛出异常。
6. **不得以 `LocalDisplayAdapter.mDozeBrightness` 作为兜底**。该值仅在屏幕 ON 状态下
   由 `animateScreenBrightness()` 写入，可能保存着日光亮度。
7. **不得以 jar 哈希判断改动是否生效**。`build.sh` 不可字节复现：同一输入构建两次，
   `classes2.dex` 的 sha256 即不相同（smali/dex 顺序重排）。对比 `baksmali` 输出
   才有意义。但 Magisk 安装器仅校验**原厂** jar 的哈希，不受影响。
8. **固件绑定**。匿名类索引 `$1`、`val$*` 字段、两个方法签名、
   `-$$Nest$fgetmPhysicalDisplayId` 均为硬编码基线。更换固件前应先反汇编核对，
   任何一项不符即应停止，不得强行修改。

## 与本仓库自动脚本的对照

| 本指南步骤 | 对应脚本 |
| --- | --- |
| 第三步编译桥接类 | `build.sh` 第 1/5 步 |
| 第四步 7.1–7.4 全部手工改动 | `patch_smali.py`（自动注入 + 签名强校验，任一项不符即报错） |
| 第五步回编译 | `build.sh` 第 4/5 步（`smali assemble -a 29`） |
| 第六步重打包 | `repack_jar.py` |

如果环境允许，应优先使用 `./build.sh /path/to/stock/services.jar`——自动化流程在签名
不匹配时**必须**报错，手工操作则容易遗漏。本指南用于理解原理与排查问题，是自动化
流程的手工对照。
