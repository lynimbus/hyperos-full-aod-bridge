# 手动修补指南

> 版本绑定：Xiaomi 14（`houji`）/ Android 16 / HyperOS OS3.1
> 下文所有方法签名、字段名、`.registers` 均来自该固件；更换固件前先反汇编核对，不符即停止。
> 本指南是 `build.sh` + `patch_smali.py` 的手工等价操作：smali 内容与脚本逐字一致，
> 环境允许时仍优先用脚本；手工流程与脚本的对应关系见文末对照表。

## 改动总览

| 步骤 | 加载dex | 定位 | 操作 |
| --- | --- | --- | --- |
| 2.1 | classes2.dex | `LocalDisplayAdapter$LocalDisplayDevice$1.smali` → 方法 `setDisplayBrightness(FF)V` 定义 | 修改：方法名加 `aodBridge` 前缀 |
| 2.2 | classes2.dex | 同一文件 → 方法 `setDisplayState(I)V` 定义开头 | 增加：边沿记录 + 预点亮调用 |
| 2.3 | classes2.dex | 同一文件 → 末尾最后一个 `.end method` 之后 | 增加：`aodBridgeDisplayId()` 与 `setDisplayBrightness` 包装方法 |
| 2.4 | classes2.dex | 目录 `com/android/server/display/` | 增加：放入 `AodDozeBridge.smali`（附录 A） |
| 3 | classes2.dex（本机；其它固件见 §0 定位） | `DisplayPowerController.smali` → 预点亮钩子（§3，新旧固件两种定位） | 增加：`setDozeBrightness(F)` 调用（可选，缺了退回亮度边沿点亮） |

## 0. 准备与定位

材料：
- 本机原厂 `services.jar`：`adb pull /system/framework/services.jar`（或从 ROM 包里取）；
- 备份原 jar；刷机前确保手里有可完整回退的原厂包。

**本固件（OS3.1 / houji）只需动 `classes2.dex` 一个文件**：
`LocalDisplayAdapter$LocalDisplayDevice$1` 与 `DisplayPowerController` 都在里面，
且钩子是「新固件」形态（`updatePowerStateInternal`，见 §3b）。其它固件先定位再动手：

（定位：`unzip -p services.jar classes2.dex | strings | grep -E 'updateAodAutoBrightness|updatePowerStateInternal'`，
再用 `baksmali disassemble -a 36` 反汇编后核对。）

## 1. 桥接类（新增文件的来源）

新增的 `AodDozeBridge.smali` 不需要重新编译：直接使用**附录 A** 全文
（与 `build.sh` 步骤 1 的产物逐字一致，来源是 `.work/bridge/com/android/server/display/AodDozeBridge.smali`）。
§2.4 在目标目录新建文件、粘贴全文即可。

想从 Java 源码重新生成（例如调大 `PANEL_SETTLE_MS` / `COMMIT_DELAY_MS`）时，在电脑上执行：

```bash
javac -nowarn -XDstringConcat=inline -d work/classes \
  stubs/com/android/server/display/*.java src/com/android/server/display/*.java
d8 --release --min-api 36 --no-desugaring --output work/dex \
  work/classes/com/android/server/display/AodDozeBridge.class
baksmali disassemble -a 36 -o work/bridge work/dex/classes.dex
```

产物：`work/bridge/com/android/server/display/AodDozeBridge.smali`，用它替换附录 A 后再继续。
`-XDstringConcat=inline` 必须带，原因见[禁忌](#6-禁忌清单)第 1 条。

## 2. 修改 LocalDisplayAdapter$LocalDisplayDevice$1.smali（classes2.dex）

在反编译出的目录里打开
`com/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1.smali`，
用内置文本编辑器逐处修改（2.1 改一行、2.2 整段插入、2.3 整段追加），改完保存。

先核对前置条件，以下字段每项必须恰好出现一次，不符即停止：

```text
.field final synthetic val$oldState:I
.field final synthetic val$state:I
.field final synthetic val$token:Landroid/os/IBinder;
.field final synthetic this$1:Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;
```

另需确认 `LocalDisplayAdapter$LocalDisplayDevice.smali` 中存在
`-$$Nest$fgetmPhysicalDisplayId` 与 `getDisplayId(J)I`，且整个文件**没有**
`AodDozeBridge` 字样（防重复打补丁）。

### 2.1 修改：原方法改名

定位：方法 `setDisplayBrightness(FF)V` 的定义。

```smali
.method private setDisplayBrightness(FF)V
    .registers 16
    .param p1, "brightnessState"    # F
    .param p2, "sdrBrightnessState"    # F
```

修改：第一行改为 `aodBridgeSetDisplayBrightness(FF)V`。

`.registers` 原样保留（本机为 **16**；示例数字随固件变，只改方法名，寄存器数、参数、方法体
一律不动）。类内原有调用点写的仍是
`setDisplayBrightness(FF)V`，重命名后自动落到 2.3 新增的同名包装方法上——此为"包装"技巧，
无需改任何调用点。

### 2.2 增加：setDisplayState 开头注入

定位：方法 `setDisplayState(I)V` 的定义。

```smali
.method private setDisplayState(I)V
    .registers 13
    .param p1, "state"    # I
```

增加：在 `.param` 行之后、原方法第一条指令之前插入（仅用 v0–v3，`.registers` 不动；
本机为 13，条件 `registers - 2 >= 4` 须满足）：

```smali
    invoke-direct {p0}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->aodBridgeDisplayId()I

    move-result v0

    iget v1, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->val$oldState:I

    move v2, p1

    invoke-static {v0, v1, v2}, Lcom/android/server/display/AodDozeBridge;->onDisplayState(III)F

    move-result v2

    const/4 v3, 0x0

    cmpl-float v3, v2, v3

    if-lez v3, :aod_bridge_no_prelight

    invoke-direct {p0, v2, v2}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->setDisplayBrightness(FF)V

    :aod_bridge_no_prelight
```

`onDisplayState` 记录 OFF→DOZE 边沿并返回桥接类里提前保存的息屏亮度（无值为 NaN，
`cmpl-float` 结果为负即跳过）；返回值 > 0 时直接调包装方法完成 NORMAL→写亮度→DOZE 整轮，
`.catchall` 保证异常也回 DOZE。

### 2.3 增加：文件末尾追加包装方法与辅助方法

定位：文件最后一个 `.end method` 之后，整段追加：

```smali
.method private aodBridgeDisplayId()I
    .registers 4

    iget-object v0, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->this$1:Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;

    invoke-static {v0}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;->-$$Nest$fgetmPhysicalDisplayId(Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;)J

    move-result-wide v1

    invoke-virtual {v0, v1, v2}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice;->getDisplayId(J)I

    move-result v0

    return v0
.end method

.method private setDisplayBrightness(FF)V
    .registers 9
    .param p1, "brightnessState"    # F
    .param p2, "sdrBrightnessState"    # F

    invoke-direct {p0}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->aodBridgeDisplayId()I

    move-result v0

    iget v1, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->val$state:I

    iget-object v2, p0, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->val$token:Landroid/os/IBinder;

    invoke-static {v0, v1, v2, p1}, Lcom/android/server/display/AodDozeBridge;->beginBrightness(IILandroid/os/IBinder;F)F

    move-result v3

    const/4 v4, 0x0

    cmpl-float v4, v3, v4

    if-lez v4, :aod_bridge_original

    goto :aod_bridge_skip

    :aod_bridge_original
    :try_start_aod_bridge
    invoke-direct {p0, p1, p2}, Lcom/android/server/display/LocalDisplayAdapter$LocalDisplayDevice$1;->aodBridgeSetDisplayBrightness(FF)V
    :try_end_aod_bridge
    .catchall {:try_start_aod_bridge .. :try_end_aod_bridge} :catchall_aod_bridge

    :aod_bridge_skip
    invoke-static {}, Lcom/android/server/display/AodDozeBridge;->endBrightness()V

    return-void

    :catchall_aod_bridge
    move-exception v5

    invoke-static {}, Lcom/android/server/display/AodDozeBridge;->endBrightness()V

    throw v5
.end method
```

- 这里的 `.registers 9` 是**新建方法自己的**寄存器数（v0–v5 六个局部 + p0–p2 三个参数 = 9），
  与 2.1 原方法的 16 无关，照抄本段即可，不要改成原方法的值。
- `aodBridgeDisplayId()I`：`mPhysicalDisplayId` 是私有字段，只能经嵌套类合成的
  `-$$Nest$fgetmPhysicalDisplayId` 访问，再用 `getDisplayId(J)` 转成逻辑 displayId。
- 包装方法：`beginBrightness(...)` 返回 > 0 表示桥已接管并**直接写好了面板亮度**
  （`SurfaceControl.setDisplayBrightness`），包装**跳过原方法**（`goto :aod_bridge_skip`）；
  返回 0 表示本边沿无 relight（框架自己的亮度请求），按原样调用原方法。
- 为什么不能调用原方法 relight：Full AOD 状态下原方法走"正常亮度"分支并调用
  `updateDozeBrightness(0)` 把 doze 亮度清零，实机 dmesg 显示 relight 时刻写的是
  `backlight 0`（面板不亮）。
- LHBM 补写：桥写亮度后等 `LHBM_WINDOW_MS`（400ms）再补写一次。点击屏幕唤醒 AOD 会触发
  指纹认证的 Local HBM 窗口（~360ms），窗口内内核拒绝一切背光写入（`due to LHBM is on`），
  且框架事后不重发亮度——不补写面板会一直黑，直到环境光变化触发重算。
- `.catchall`：原方法体抛异常时也必须 `endBrightness()` 把面板送回 DOZE，否则停留在
  NORMAL 常亮。异常先存 v5 再 `throw v5`。
- 标签 `aod_bridge_*` 仅要求与本文件既有标签不重名。

### 2.4 增加：放置桥接类

在 `com/android/server/display/` 目录下新建文件 `AodDozeBridge.smali`，
粘贴**附录 A** 全文（或拷贝 `.work/bridge/com/android/server/display/AodDozeBridge.smali`
进目录）。

核对：搜索整个 `$1.smali`，`AodDozeBridge` 应恰好出现 4 处（`onDisplayState`、`beginBrightness`、
两条路径上的 `endBrightness`）。桥接类自己不算。

## 3. （可选）DisplayPowerController 预点亮钩子

缺失时补丁仍可用（退回亮度请求边沿点亮，与旧版行为一致），但 v3 的延迟优化不生效。
本固件走 **3b**（新固件）。按固件代际二选一：

### 3a. 旧固件（Android 16 亮度重构之前）

定位：`DisplayPowerController.smali`，方法 `updateAodAutoBrightness`
内的日志拼接处——`const-string` 含 `"updateAodAutoBrightness"` 之后的第一个
`StringBuilder;->append(F)` 指令。

增加：在该 `append(F)` 之前插入一行，寄存器换成 `append(F)` 的第二个操作数
（即 `newAodScreenAutoBrightness` 所在寄存器）：

```smali
    invoke-static {v0}, Lcom/android/server/display/AodDozeBridge;->setDozeBrightness(F)V
```

### 3b. 新固件（Android 16 亮度重构）

定位：`DisplayPowerController.smali`，方法 `updatePowerStateInternal()V` 内第一处
`DisplayBrightnessState;->getBrightness()F` 之后的 `move-result` 指令——该寄存器就是框架
算好的最终亮度（`DisplayBrightnessController` 策略链输出，含 DOZE 策略；状态边沿时会被
桥消费，普通亮度更新喂了也无害）。本固件实机形态（`move-result v4` 之后插一行）：

```smali
    invoke-virtual {v3}, Lcom/android/server/display/DisplayBrightnessState;->getBrightness()F

    move-result v4

    invoke-static {v4}, Lcom/android/server/display/AodDozeBridge;->setDozeBrightness(F)V
```

> 注入点位于状态派发（`animateScreenStateChange` → `PhotonicModulator`）的同一轮
> `updatePowerState` 内、状态请求到达 `LocalDisplayAdapter` 之前（`PhotonicModulator`
> 等到亮度非 NaN 才派发），因此预点亮值先于状态边沿就绪。

## 4. 编译与回填 jar

1. 反编译出的 smali 目录 → **编译** → 在同目录生成新的 dex
   （沿用原 dex 版本）。
2. 回到 `services.jar` 的 zip 视图：长按旧 `classes2.dex` → 删除；再把编译出的新 dex
   **添加 / 替换**进 jar。若询问压缩方式，必须选**存储（不压缩）**；其它 dex 条目原样不动。
3. 核对：zip 视图长按 `classes2.dex` → 属性，压缩方式应为「存储」。

**为什么必须 stored + 4 字节对齐**：`services.jar` 在 boot classpath 上，ART 直接从 jar 里
mmap dex，没有可写的提取路径；压缩或未对齐的条目会导致 ART 加载失败 → system_server 起不来
（开机卡死）。

- 刷入后用 §5 的 logcat 证明 dex 被正常加载（出现 `AodDozeBridge` 日志即成功）；

## 5. 刷入与验证

### 方式 A：Magisk 模块（推荐）

新建目录 `module/`，内容：

- `system/framework/services.jar` —— 补丁后的 jar（文件名保持 `services.jar`）；
- `module.prop` —— 与仓库 `magisk/module.prop` 一致：

```text
id=aod_doze_bridge_rom
name=HyperOS Full AOD Bridge (framework)
version=v3
versionCode=3
author=lynimbus
description=请使用元模块挂载并关闭默认卸载模块
```

- `customize.sh` —— 与仓库 `magisk/customize.sh` 一致：

```bash
SKIPUNZIP=0

ui_print "- 设备: $(getprop ro.product.device)"
ui_print "- 版本: $(getprop ro.build.version.incremental)"

set_perm_recursive "$MODPATH/system/framework" 0 0 0755 0644
ui_print "- 已就位，重启后生效"
```

把 `module/` 压缩为 zip（模块 zip 的压缩方式无所谓，Magisk 会解压），
Magisk → 模块 → 从本地安装 → 重启。

### 方式 B：直接替换

覆盖 `/system/framework/services.jar`，删除 `oat/arm64/services.{odex,vdex,art}` 与
`.fsv_meta`，按官改包流程处理 AVB / dm-verity。

### 验证

```bash
adb logcat -s AodDozeBridge                      # 桥接日志
adb shell su -c 'dmesg | grep -iE "backlight|LP1"'   # 亮度写入是否到达面板
```

正常预点亮恢复产生两行，时间戳几乎同时：

```text
armed OFF->DOZE edge, display=0 state=3 prelight=0.0304
relighting display 0 state=3 with brightness 0.0304
```

`prelight=none` 表示该次退回亮度请求边沿（两行拉开约 70 ms，仍健康）。dmesg 中
`relighting` 之后出现 `set 51 backlight N` 为成功；出现
`skip set backlight ... due to LP1 on` 说明延迟不足，调大 `AodDozeBridge.java` 的
`PANEL_SETTLE_MS`（默认 16）/ `COMMIT_DELAY_MS`（默认 64）后从第 1 节重编。framework
自身每次恢复的亮度写入都会被 LP1 拒绝，那是原厂行为，不是回归。

如果**既没有**桥接日志、**也没有**框架自己那条无条件的
`setDisplayState(id=..., state=DOZE)` 日志，进程死在注入点内——检查
`adb logcat -b crash -d`，而不是按 tag 过滤。

## 6. 禁忌清单

1. **严禁 `invoke-custom` / `invoke-polymorphic` 进 dex。** `services.jar` 在 boot
   classpath 上，ART 解析 `StringConcatFactory` 的 bootstrap method 时 `Runtime::Abort`
   （native abort，Java `catch` 与 smali `.catchall` 都拦不住），system_server 直接崩溃。
   桥接类必须**整段**使用附录 A（d8 产物，已按 `-XDstringConcat=inline` 编译），不要手工
   改写它、更不要手写字符串拼接逻辑。改完确认附录 A 全文搜不到这两个指令。
2. **不得调小 `setDisplayState` 的 `.registers`**（本机 13；注入只用 v0–v3，固件变了则
   `registers - 2` 仍须 ≥ 4）。
3. **`.catchall` 不可删**——异常路径也必须 `endBrightness()` 回 DOZE，否则面板常亮。
4. **边沿必须跨事件记忆**：状态请求携带的亮度是 `-1.0`，真实亮度约 70 ms 后单独送达；
   预点亮钩子只是让它提前到达，钩子缺失时仍要等亮度请求。不要"简化"回单点注入。
5. **桥接类每个入口吞掉 Throwable 降级 no-op**，不得抛异常（框架不匹配也不能 bootloop）。
6. **不得用 `LocalDisplayAdapter.mDozeBrightness` 兜底**（仅 ON 状态写入，可能存日光亮度）。
7. **固件绑定**：`$1` 匿名类索引、`val$*` 字段、两个方法签名、
   `-$$Nest$fgetmPhysicalDisplayId` 均硬编码，换固件先核对，不符即停。
8. **回填 jar 时 dex 条目必须「存储」且 4 字节对齐**（§4），否则 bootloop。

## 附录 A：AodDozeBridge.smali（v3 完整内容）

> 与 `build.sh` 步骤 1 的 d8 产物逐字一致（`-XDstringConcat=inline`）。
> 若用第 1 节重新生成过，以新产物为准。文件里对 `DisplayFeatureManagerServiceStub` 的
> 引用（Full AOD 判定）指向 `services.jar` 自带的同名类，无需额外补任何类。

```smali
.class public final Lcom/android/server/display/AodDozeBridge;
.super Ljava/lang/Object;
.source "AodDozeBridge.java"


# static fields
.field private static final COMMIT_DELAY_MS:J = 0x40L

.field private static final LHBM_WINDOW_MS:J = 0x190L

.field private static final NO_EDGE:I = -0x1

.field private static final PANEL_SETTLE_MS:J = 0x10L

.field private static final POWER_MODE_DOZE:I = 0x1

.field private static final POWER_MODE_NORMAL:I = 0x2

.field private static final STATE_DOZE:I = 0x3

.field private static final STATE_DOZE_SUSPEND:I = 0x4

.field private static final STATE_OFF:I = 0x1

.field private static final STATE_ON:I = 0x2

.field public static final TAG:Ljava/lang/String; = "AodDozeBridge"

.field private static volatile sArmedToken:Landroid/os/IBinder; = null

.field private static volatile sDozeBrightnessInput:F = NaNf

.field private static volatile sPendingEdge:I = -0x1


# direct methods
.method static constructor <clinit>()V
    .registers 0

    return-void
.end method

.method private constructor <init>()V
    .registers 1

    .line 131
    invoke-direct {p0}, Ljava/lang/Object;-><init>()V

    return-void
.end method

.method public static beginBrightness(IILandroid/os/IBinder;F)F
    .registers 4

    .line 207
    :try_start_0
    invoke-static {p0, p1, p2, p3}, Lcom/android/server/display/AodDozeBridge;->beginBrightnessImpl(IILandroid/os/IBinder;F)F

    move-result p0
    :try_end_4
    .catchall {:try_start_0 .. :try_end_4} :catchall_5

    return p0

    :catchall_5
    move-exception p0

    .line 210
    const-string p1, "beginBrightness failed"

    invoke-static {p1, p0}, Lcom/android/server/display/AodDozeBridge;->warn(Ljava/lang/String;Ljava/lang/Throwable;)V

    const/4 p0, 0x0

    return p0
.end method

.method private static beginBrightnessImpl(IILandroid/os/IBinder;F)F
    .registers 9

    const-string v0, "relighting display "

    .line 217
    sget v1, Lcom/android/server/display/AodDozeBridge;->sPendingEdge:I

    const/4 v2, 0x0

    if-ne v1, p0, :cond_ad

    invoke-static {p1}, Lcom/android/server/display/AodDozeBridge;->isDoze(I)Z

    move-result v1

    if-eqz v1, :cond_ad

    if-nez p2, :cond_11

    goto/16 :goto_ad

    :cond_11
    const/4 v1, -0x1

    .line 221
    sput v1, Lcom/android/server/display/AodDozeBridge;->sPendingEdge:I

    .line 223
    invoke-static {p3}, Lcom/android/server/display/AodDozeBridge;->isUsable(F)Z

    move-result v1

    if-nez v1, :cond_37

    .line 224
    new-instance p1, Ljava/lang/StringBuilder;

    const-string p2, "OFF->DOZE edge without usable brightness, display="

    invoke-direct {p1, p2}, Ljava/lang/StringBuilder;-><init>(Ljava/lang/String;)V

    invoke-virtual {p1, p0}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object p0

    const-string p1, " brightness="

    invoke-virtual {p0, p1}, Ljava/lang/StringBuilder;->append(Ljava/lang/String;)Ljava/lang/StringBuilder;

    move-result-object p0

    invoke-virtual {p0, p3}, Ljava/lang/StringBuilder;->append(F)Ljava/lang/StringBuilder;

    move-result-object p0

    invoke-virtual {p0}, Ljava/lang/StringBuilder;->toString()Ljava/lang/String;

    move-result-object p0

    invoke-static {p0}, Lcom/android/server/display/AodDozeBridge;->info(Ljava/lang/String;)V

    return v2

    :cond_37
    const/4 v1, 0x2

    .line 232
    :try_start_38
    invoke-static {p2, v1}, Landroid/view/SurfaceControl;->setDisplayPowerMode(Landroid/os/IBinder;I)V
    :try_end_3b
    .catchall {:try_start_38 .. :try_end_3b} :catchall_9a

    .line 237
    sput-object p2, Lcom/android/server/display/AodDozeBridge;->sArmedToken:Landroid/os/IBinder;

    const-wide/16 v3, 0x10

    .line 238
    invoke-static {v3, v4}, Landroid/os/SystemClock;->sleep(J)V

    .line 243
    :try_start_42
    invoke-static {p2, p3}, Landroid/view/SurfaceControl;->setDisplayBrightness(Landroid/os/IBinder;F)Z

    .line 244
    new-instance v1, Ljava/lang/StringBuilder;

    invoke-direct {v1, v0}, Ljava/lang/StringBuilder;-><init>(Ljava/lang/String;)V

    invoke-virtual {v1, p0}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object v0

    const-string v1, " state="

    invoke-virtual {v0, v1}, Ljava/lang/StringBuilder;->append(Ljava/lang/String;)Ljava/lang/StringBuilder;

    move-result-object v0

    invoke-virtual {v0, p1}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object p1

    const-string v0, " with brightness "

    invoke-virtual {p1, v0}, Ljava/lang/StringBuilder;->append(Ljava/lang/String;)Ljava/lang/StringBuilder;

    move-result-object p1

    invoke-virtual {p1, p3}, Ljava/lang/StringBuilder;->append(F)Ljava/lang/StringBuilder;

    move-result-object p1

    invoke-virtual {p1}, Ljava/lang/StringBuilder;->toString()Ljava/lang/String;

    move-result-object p1

    invoke-static {p1}, Lcom/android/server/display/AodDozeBridge;->info(Ljava/lang/String;)V
    :try_end_69
    .catchall {:try_start_42 .. :try_end_69} :catchall_86

    const-wide/16 v0, 0x190

    .line 252
    invoke-static {v0, v1}, Landroid/os/SystemClock;->sleep(J)V

    .line 254
    :try_start_6e
    invoke-static {p2, p3}, Landroid/view/SurfaceControl;->setDisplayBrightness(Landroid/os/IBinder;F)Z
    :try_end_71
    .catchall {:try_start_6e .. :try_end_71} :catchall_72

    return p3

    :catchall_72
    move-exception p1

    .line 256
    new-instance p2, Ljava/lang/StringBuilder;

    const-string v0, "cannot re-set brightness for display "

    invoke-direct {p2, v0}, Ljava/lang/StringBuilder;-><init>(Ljava/lang/String;)V

    invoke-virtual {p2, p0}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object p0

    invoke-virtual {p0}, Ljava/lang/StringBuilder;->toString()Ljava/lang/String;

    move-result-object p0

    invoke-static {p0, p1}, Lcom/android/server/display/AodDozeBridge;->warn(Ljava/lang/String;Ljava/lang/Throwable;)V

    return p3

    :catchall_86
    move-exception p1

    .line 247
    new-instance p2, Ljava/lang/StringBuilder;

    const-string p3, "cannot set brightness for display "

    invoke-direct {p2, p3}, Ljava/lang/StringBuilder;-><init>(Ljava/lang/String;)V

    invoke-virtual {p2, p0}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object p0

    invoke-virtual {p0}, Ljava/lang/StringBuilder;->toString()Ljava/lang/String;

    move-result-object p0

    invoke-static {p0, p1}, Lcom/android/server/display/AodDozeBridge;->warn(Ljava/lang/String;Ljava/lang/Throwable;)V

    return v2

    :catchall_9a
    move-exception p1

    .line 234
    new-instance p2, Ljava/lang/StringBuilder;

    const-string p3, "cannot enter NORMAL for display "

    invoke-direct {p2, p3}, Ljava/lang/StringBuilder;-><init>(Ljava/lang/String;)V

    invoke-virtual {p2, p0}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object p0

    invoke-virtual {p0}, Ljava/lang/StringBuilder;->toString()Ljava/lang/String;

    move-result-object p0

    invoke-static {p0, p1}, Lcom/android/server/display/AodDozeBridge;->warn(Ljava/lang/String;Ljava/lang/Throwable;)V

    :cond_ad
    :goto_ad
    return v2
.end method

.method public static endBrightness()V
    .registers 3

    .line 268
    :try_start_0
    sget-object v0, Lcom/android/server/display/AodDozeBridge;->sArmedToken:Landroid/os/IBinder;

    if-nez v0, :cond_5

    return-void

    :cond_5
    const/4 v1, 0x0

    .line 272
    sput-object v1, Lcom/android/server/display/AodDozeBridge;->sArmedToken:Landroid/os/IBinder;

    const-wide/16 v1, 0x40

    .line 273
    invoke-static {v1, v2}, Landroid/os/SystemClock;->sleep(J)V

    const/4 v1, 0x1

    .line 274
    invoke-static {v0, v1}, Landroid/view/SurfaceControl;->setDisplayPowerMode(Landroid/os/IBinder;I)V
    :try_end_11
    .catchall {:try_start_0 .. :try_end_11} :catchall_12

    return-void

    :catchall_12
    move-exception v0

    .line 276
    const-string v1, "endBrightness failed"

    invoke-static {v1, v0}, Lcom/android/server/display/AodDozeBridge;->warn(Ljava/lang/String;Ljava/lang/Throwable;)V

    return-void
.end method

.method private static info(Ljava/lang/String;)V
    .registers 2

    .line 298
    const-string v0, "AodDozeBridge"

    invoke-static {v0, p0}, Landroid/util/Slog;->i(Ljava/lang/String;Ljava/lang/String;)I

    return-void
.end method

.method private static isDoze(I)Z
    .registers 2

    const/4 v0, 0x3

    if-eq p0, v0, :cond_9

    const/4 v0, 0x4

    if-ne p0, v0, :cond_7

    goto :goto_9

    :cond_7
    const/4 p0, 0x0

    return p0

    :cond_9
    :goto_9
    const/4 p0, 0x1

    return p0
.end method

.method private static isFullAod(I)Z
    .registers 3

    const/4 v0, 0x0

    .line 290
    :try_start_1
    invoke-static {}, Lcom/android/server/display/DisplayFeatureManagerServiceStub;->getInstance()Lcom/android/server/display/DisplayFeatureManagerServiceStub;

    move-result-object v1

    if-eqz v1, :cond_f

    .line 291
    invoke-interface {v1, p0}, Lcom/android/server/display/DisplayFeatureManagerServiceStub;->isFullAodState(I)Z

    move-result p0
    :try_end_b
    .catchall {:try_start_1 .. :try_end_b} :catchall_f

    if-eqz p0, :cond_f

    const/4 p0, 0x1

    return p0

    :catchall_f
    :cond_f
    return v0
.end method

.method private static isUsable(F)Z
    .registers 2

    .line 285
    invoke-static {p0}, Ljava/lang/Float;->isNaN(F)Z

    move-result v0

    if-nez v0, :cond_13

    invoke-static {p0}, Ljava/lang/Float;->isInfinite(F)Z

    move-result v0

    if-nez v0, :cond_13

    const/4 v0, 0x0

    cmpl-float p0, p0, v0

    if-lez p0, :cond_13

    const/4 p0, 0x1

    return p0

    :cond_13
    const/4 p0, 0x0

    return p0
.end method

.method public static onDisplayState(III)F
    .registers 9

    const-string v0, " prelight="

    const-string v1, "armed OFF->DOZE edge, display="

    const/4 v2, -0x1

    const/high16 v3, 0x7fc00000    # Float.NaN

    const/4 v4, 0x1

    if-eq p2, v4, :cond_58

    const/4 v5, 0x2

    if-ne p2, v5, :cond_e

    goto :goto_58

    :cond_e
    if-ne p1, v4, :cond_57

    .line 154
    :try_start_10
    invoke-static {p2}, Lcom/android/server/display/AodDozeBridge;->isDoze(I)Z

    move-result p1

    if-eqz p1, :cond_57

    invoke-static {p0}, Lcom/android/server/display/AodDozeBridge;->isFullAod(I)Z

    move-result p1

    if-eqz p1, :cond_57

    .line 155
    sput p0, Lcom/android/server/display/AodDozeBridge;->sPendingEdge:I

    .line 159
    sget p1, Lcom/android/server/display/AodDozeBridge;->sDozeBrightnessInput:F

    .line 160
    sput v3, Lcom/android/server/display/AodDozeBridge;->sDozeBrightnessInput:F

    .line 161
    new-instance v4, Ljava/lang/StringBuilder;

    invoke-direct {v4, v1}, Ljava/lang/StringBuilder;-><init>(Ljava/lang/String;)V

    invoke-virtual {v4, p0}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object p0

    const-string v1, " state="

    invoke-virtual {p0, v1}, Ljava/lang/StringBuilder;->append(Ljava/lang/String;)Ljava/lang/StringBuilder;

    move-result-object p0

    invoke-virtual {p0, p2}, Ljava/lang/StringBuilder;->append(I)Ljava/lang/StringBuilder;

    move-result-object p0

    .line 162
    invoke-static {p1}, Lcom/android/server/display/AodDozeBridge;->isUsable(F)Z

    move-result p2

    if-eqz p2, :cond_49

    .line 163
    new-instance p2, Ljava/lang/StringBuilder;

    invoke-direct {p2, v0}, Ljava/lang/StringBuilder;-><init>(Ljava/lang/String;)V

    invoke-virtual {p2, p1}, Ljava/lang/StringBuilder;->append(F)Ljava/lang/StringBuilder;

    move-result-object p2

    invoke-virtual {p2}, Ljava/lang/StringBuilder;->toString()Ljava/lang/String;

    move-result-object p2

    goto :goto_4b

    :cond_49
    const-string p2, " prelight=none"

    :goto_4b
    invoke-virtual {p0, p2}, Ljava/lang/StringBuilder;->append(Ljava/lang/String;)Ljava/lang/StringBuilder;

    move-result-object p0

    invoke-virtual {p0}, Ljava/lang/StringBuilder;->toString()Ljava/lang/String;

    move-result-object p0

    .line 161
    invoke-static {p0}, Lcom/android/server/display/AodDozeBridge;->info(Ljava/lang/String;)V

    return p1

    :cond_57
    return v3

    .line 150
    :cond_58
    :goto_58
    sput v2, Lcom/android/server/display/AodDozeBridge;->sPendingEdge:I

    .line 151
    sput v3, Lcom/android/server/display/AodDozeBridge;->sDozeBrightnessInput:F
    :try_end_5c
    .catchall {:try_start_10 .. :try_end_5c} :catchall_5d

    return v3

    :catchall_5d
    move-exception p0

    .line 169
    sput v2, Lcom/android/server/display/AodDozeBridge;->sPendingEdge:I

    .line 170
    sput v3, Lcom/android/server/display/AodDozeBridge;->sDozeBrightnessInput:F

    .line 171
    const-string p1, "onDisplayState failed"

    invoke-static {p1, p0}, Lcom/android/server/display/AodDozeBridge;->warn(Ljava/lang/String;Ljava/lang/Throwable;)V

    return v3
.end method

.method public static setDozeBrightness(F)V
    .registers 3

    const/high16 v0, 0x7fc00000    # Float.NaN

    .line 188
    :try_start_2
    invoke-static {p0}, Lcom/android/server/display/AodDozeBridge;->isUsable(F)Z

    move-result v1

    if-eqz v1, :cond_9

    goto :goto_a

    :cond_9
    move p0, v0

    :goto_a
    sput p0, Lcom/android/server/display/AodDozeBridge;->sDozeBrightnessInput:F
    :try_end_c
    .catchall {:try_start_2 .. :try_end_c} :catchall_d

    return-void

    .line 190
    :catchall_d
    sput v0, Lcom/android/server/display/AodDozeBridge;->sDozeBrightnessInput:F

    return-void
.end method

.method private static warn(Ljava/lang/String;Ljava/lang/Throwable;)V
    .registers 3

    .line 302
    const-string v0, "AodDozeBridge"

    invoke-static {v0, p0, p1}, Landroid/util/Slog;->w(Ljava/lang/String;Ljava/lang/String;Ljava/lang/Throwable;)I

    return-void
.end method
```

## 与自动脚本的对照

| 本指南步骤 | 对应脚本 |
| --- | --- |
| 第 0/1 节（定位、桥接类来源） | `build.sh` 步骤 1（javac + d8 + baksmali 生成 `AodDozeBridge.smali`） |
| 2.1–2.4 与第 3 节全部手工改动 | `patch_smali.py`（2.1–2.4 硬校验、不符即报错；第 3 节尽力而为、匹配不到只告警） |
| 第 4 节编译回填 | `build.sh` 步骤 4（`smali assemble -a 29`，仅回编译被改过的 dex）+ `repack_jar.py` |
| 第 5 节 Magisk 模块 | `build.sh` 的 zip 打包（`magisk/` 目录） |
