# 解决米系手机强开全屏AOD后智能显示失效的问题

## 现象

小米从 15 系开始正式提供全屏 AOD，息屏后可以保留与锁屏界面接近的壁纸和样式。部分较早的机型虽然没有开放这个功能，系统里却已经带有相关上层代码。将安全设置中的 `full_screen_aod_on` 打开，并同时启用 `doze_always_on`，便能强制进入全屏 AOD。一些官改包和功能补全模块也是通过这个开关实现的。

强制开启后，第一次锁屏通常可以正常显示，亮度也符合当时的环境。但智能显示因为遮挡、暗光或无人注视而关闭屏幕以后，条件恢复时 AOD 可能再也不亮。切回普通 AOD，同样的遮挡和注视操作又完全正常。

实际抓取日志后可以确认，传感器和注视判断没有失效，系统也一直在计算新的息屏亮度。屏幕重新出现时，AOD 内容和显示状态已经恢复，真正丢失的是最后一步亮度写入。

本文的分析和修复方法面向使用同套小米显示框架、并出现相同症状的米系设备。小米 14 Pro 是验证和取证所用的样机，不是这套方法在代码层面的唯一目标。

## 普通 AOD 与全屏 AOD 使用了不同的亮度通道

屏幕在这里有三个容易混淆的状态。ON 是正常亮屏，OFF 是面板关闭，DOZE 则是 AOD 使用的低功耗显示状态。DOZE 不是普通亮屏，也不是彻底断电；它允许面板用较低刷新率和功耗保留息屏内容。

传统 AOD 只显示时间、通知等少量元素，不需要连续变化的亮度。小米为它准备了一条专用通道：环境亮度最终只被分成高、低两档，再以显示功能 mode 25 交给驱动。驱动认识“息屏高亮”和“息屏低亮”这两个命令，所以即使面板已经进入 DOZE，也能可靠地熄灭或恢复。

全屏 AOD 需要显示壁纸和完整锁屏样式，两档亮度很容易出现突兀跳变。小米因此保留了另一套自动亮度控制器，先根据环境光算出连续的目标亮度，再通过正常亮屏所用的通用背光通道写给屏幕。

下面是这次反编译中最关键的一处分流。代码细节并不重要，含义只有两点：普通 AOD 在 DOZE 中走专用息屏亮度接口；全屏 AOD 会绕过它，改走通用背光接口。

```java
if (!isFullAodState(displayId) && isDoze(mState)) {
    updateDozeBrightness(physicalDisplayId, brightness); // mode 25，两档
} else {
    mBacklightAdapter.setBacklight(...);                 // 通用连续亮度
}
```

普通 AOD 的实机日志只会看到 mode 25 的两个亮度状态：

```text
DisplayFeatureManagerService: mode=25 value=1
DisplayFeatureHal: DOZE_BRIGHTNESS_STATE modeId=1

DisplayFeatureManagerService: mode=25 value=2
DisplayFeatureHal: DOZE_BRIGHTNESS_STATE modeId=2
```

这里的“两档”只是传统 AOD 专用接口对上层提供的能力，并不表示 OLED 面板在物理上只能保持两种亮度。全屏 AOD 第一次锁屏时能够保持任意中间亮度，已经足以证明这一点。

## 为什么第一次锁屏正常，恢复时却是黑屏

第一次锁屏和智能显示恢复的区别不在亮度算法，而在操作顺序。

第一次锁屏时，面板仍处于正常亮屏状态。系统先写入自动亮度控制器算出的目标值，再把面板切入 DOZE。进入低功耗状态后，面板会保留刚才已经写好的亮度，因此画面和亮度都正常。

```text
第一次锁屏：ON → 写入连续亮度 → DOZE
```

智能显示关闭 AOD 后，面板会真正进入 OFF。再次满足显示条件时，原系统的顺序变成先从 OFF 直接进入 DOZE，再补写连续亮度：

```text
智能显示恢复：OFF → DOZE → 写入连续亮度
```

问题正好出在最后一步。验证样机的厂商显示组件不接受这种状态下的通用亮度写入，日志明确记录了拒绝原因：

```text
HWPeripheralDRM::SetPanelBrightness:
Power state 1 pending or aod layer 0!
Skip for setting brightness 48 level
```

后续亮度仍在计算和提交，但都被同一条件跳过。结果是屏幕已经回到 DOZE，AOD 内容也准备好了，面板亮度却仍停在关闭时的零值，于是看起来像是“智能显示没有唤醒”。关闭之所以始终有效，是因为从非零亮度降到 OFF 不依赖这次被拒绝的恢复写入；重新显示则必须成功写入一个非零亮度。

日志里的 `aod layer 0` 是厂商显示组件维护的 AOD 图层计数，不是内核设备数量，也不能用来证明屏幕只支持两档。能够确定的只是：在这套显示实现中，面板已经进入 DOZE 后，通用连续亮度命令会被状态检查挡住。

对比小米 14 Pro 与 15 Pro 的系统文件也能印证问题所在。两者负责计算 AOD 自动亮度的上层代码相同，负责选择普通或全屏亮度通道的代码也没有实质差异；厂商显示库却并不相同。也就是说，旧机型不是缺少环境光算法，而是下层显示实现没有完整配合这条全屏 AOD 恢复时序。

## 两种直观方案的局限

曾尝试在厂商显示库中修改一条四字节跳转，让亮度命令越过已经定位到的拒绝分支。实测表明，整条链路还有电源状态、异步提交和 AOD 图层状态等其他约束，绕过一处判断仍不足以建立可靠的 DOZE 连续调光。继续修改显示 HAL、内核面板驱动和面板命令表可以从根本上适配，但风险、工作量和固件依赖都很高。

另一种尝试是把全屏 AOD 强行接回传统 AOD 的 mode 25 通道。它可以重新点亮屏幕，却会把连续亮度压成明显的高、低两档，还会引入阈值和单位不一致的问题，因此也不适合作为最终方案。

## 修复方法

既然第一次锁屏能够正常工作，最简单的办法不是重写亮度算法，而是在智能显示恢复的一瞬间复现第一次锁屏的顺序。

模块继续使用小米原有的环境光算法，只记录它算出的最新亮度。当检测到全屏 AOD 发生真实的 OFF→DOZE 恢复时，先让物理面板短暂回到 ON，沿系统原来的通用背光通道写入这个亮度，等待写入被显示组件接收，再立即切回 DOZE：

```text
OFF → DOZE → 临时 ON → 写入最新亮度 → 等待一次提交 → DOZE
```

实际实现中等待时间为 16 毫秒。这不是为了等待环境光稳定，也不是人为制造亮度动画，而是因为屏幕状态和亮度命令会异步交给显示组件。若写完后立刻切回 DOZE，后一个状态命令可能先被处理，亮度仍会因为面板正在切换而遭到拒绝。16 毫秒只出现在 AOD 重新显示的边沿，不会周期运行。

LSPosed 模块在 `system_server` 中观察三类信息：当前是否为全屏 AOD、小米自动亮度控制器给出的最终亮度、物理屏幕是否刚从 OFF 恢复到 DOZE。只有三项条件同时成立时，才执行一次临时 ON、写亮度、回到 DOZE 的操作。模块不接管环境光曲线，不自行换算 nit 或 DBV，也不修改普通 AOD 和正常亮屏的亮度。

环境光在 AOD 已经显示期间继续变化时，原控制器仍会计算新值，模块只更新缓存，不反复唤醒面板。下一次智能显示先隐藏、再恢复时，才使用当时最新的亮度。因此它实现的是“每次重新显示时自适应”，不是 AOD 显示期间持续追踪环境光。这样既保留了连续亮度，也避免为了实时调光频繁切换面板状态。

## 适用范围

模块不包含驱动文件、二进制偏移或机型专用面板命令，核心依赖是小米系统框架中全屏 AOD 的分流逻辑、自动亮度控制器和物理显示状态回调。只要系统仍保留这些结构，并且故障表现同样是“首次显示正常、OFF 后恢复到 DOZE 时亮度写入被拒绝”，就可以尝试使用，不必严格限定为某一个机型或完整版本号。

目前完成实机验证的环境是小米 14 Pro（`shennong`）、Android 16、`OS3.0.307.0.WNBCNXM`，MIUI AOD 版本号 `22335101`。相近的 HyperOS 版本如果相关类和字段没有变化，模块通常可以直接加载；结构发生变化时，模块会停止安装钩子而不执行修正，需要按该版本重新适配类名或字段名。

这不是全屏 AOD 开启模块。设备仍需通过系统设置、官改包或其他补全模块启用 `full_screen_aod_on`。如果普通 AOD 也无法恢复、日志不是 DOZE 中的亮度写入被拒绝，故障原因便不属于本文讨论的范围。

## 使用方法

模块需要 root、LSPosed 和支持 libxposed API 101 的管理器。安装后在 LSPosed 中启用模块，作用域只选择“系统框架（System Framework / system）”，然后重启设备。公开版使用新的通用应用 ID；安装过内部测试版时，应先停用并卸载旧模块，避免两个版本同时挂钩。

模块下载：[GitHub Releases](https://github.com/silverpoetry/hyperos-full-aod-bridge/releases)

开源地址：[hyperos-full-aod-bridge](https://github.com/silverpoetry/hyperos-full-aod-bridge)
